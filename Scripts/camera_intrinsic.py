     # import math 
        # height = 1280
        # width = 720
        # cam_id = mujoco.mj_name2id(self.mjmodel, mujoco.mjtObj.mjOBJ_CAMERA, 'd435i_camera_rgb')
        # fovy = self.mjmodel.cam_fovy[cam_id]
        # f = 0.5 * height / math.tan(fovy * math.pi / 360)
        # print(np.array(((f, 0, width / 2), (0, f, height / 2), (0, 0, 1))))